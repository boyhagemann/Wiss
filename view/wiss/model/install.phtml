<h1>Install model <?= $title ?></h1>

<?= $this->form()->openTag($form) ?>

<div class="tabbable">
	<ul class="nav nav-tabs">
		<li class="active"><a href="#model-properties" data-toggle="tab">Properties</a>
		<li><a href="#model-elements" data-toggle="tab">Elements</a>
	</ul>
	<div class="tab-content">
		<div class="tab-pane active" id="model-properties">
			<?= $this->formElementWrapper($form->get('title')) ?>
			<?= $this->formElementWrapper($form->get('entity_class')) ?>
			<?= $this->formElementWrapper($form->get('title_field')) ?>
		</div>
		
		<div class="tab-pane" id="model-elements">
			<ul class="unstyled list">
			<?php foreach($form->get('elements') as $element) : ?>
				<li>
					<h2><?= $element->getOption('legend') ?></h2>
					<?php foreach($element as $child) : ?> 
					<?= $this->formRow($child) ?>
					<?php endforeach ?>		
						<a href="" class="btn btn-small">Configure</a>
				</li>
			<?php endforeach ?>
			</ul>
		</div>
	</div>
</div>
			
<div class="form-actions">
	<?= $this->formElementWrapper($form->get('submit')) ?>
</div>

<?= $this->form()->closeTag() ?>